# H5 继续修改交接说明

## 1. 视觉与素材原则

完整设计图和 PSD 是唯一视觉标准。除非用户明确提出新的位置调整，否则：

1. 不改变人物、背景、文字、气泡、手机和装饰元素的位置、大小、比例、颜色与层级。
2. 不重新排版，不重新生成素材，不使用字体重新绘制 PSD 文字。
3. 不裁切人物，不拉伸图片，不使用 `object-fit: cover` 处理原始场景图。
4. 场景内部素材继续使用绝对定位和 PSD 原坐标。
5. 手机适配只允许对完整场景整体等比缩放。

## 2. 坐标体系

### 前半段

- 固定设计宽度：1080px。
- 原始高度：13549px。
- 场景：`opening`、`introduction`、`bedroom`、`classroom`、`school-road`、`transition`、`online-world`。
- PSD 场景及图层数据：`assets/manifest.js`、`assets/interactions.js`。

### 后半段

- 原始设计宽度：750px。
- 通过 `1.44` 倍整体缩放接入 1080px 坐标系。
- 起始设计坐标：13549px。
- 配置：`assets/extension/manifest.js`。
- 总设计高度约：34006px。

## 3. 手机端当前分页规则

启用条件：视口宽度不超过 600px。

普通章节采用独立翻页，不是长卷轴：

1. 开头
2. 介绍小羽
3. 卧室
4. 教室
5. 放学路
6. 手机过渡
7. 采访引入＋三个家庭（唯一连续滚动区）
8. 三个家庭只是三个切面
9. 首次接触网络年龄
10. 使用时长和用途
11. 健康风险
12. 成年人态度
13. 政策地图
14. 结尾

注意：`online-world` 不再是一个独立翻页节点。它只能通过点击手机出现，避免“小羽的网络环境”重复展示两次。

分页控制文件：`mobile-pager.js`、`mobile-pager.css`。

## 4. 当前重点交互

### 开头

电脑图标、`1.96亿`、`未成年`、`过早上网的一代`按既定时间线从上向下依次落入最终位置。

### 卧室、教室、放学路

进入页面后依次出现：

1. 环境元素。
2. 人物。
3. 第一条气泡。
4. 用户点击提示后显示第二条气泡。

动画结束时所有素材回到 PSD 最终坐标。

### 手机与小羽的网络环境

点击手机后：

1. 手机外壳淡出。
2. 信息流放大并变清晰。
3. 小羽渐显。
4. 小羽上方五条文字依次出现。

手机端网络环境会从视口顶部开始，整组等比放大至覆盖手机可视区域。此处是用户最新明确要求；为覆盖不同长宽比屏幕，画面两侧可能超出可视区域。

### 三个家庭采访

采访引入和三个家庭是唯一连续滚动区。`mobile-pager.js` 中使用手指拖动距离直接推进滚动位置，避免 iOS 在分页锁定与原生滚动切换时卡住。滑到第三个家庭底部后继续上滑，进入下一独立页。

### “三个家庭，只是三个切面”

该页是唯一居中对齐的短过渡页：

- 图层 270 在 750px 场景内水平、垂直居中。
- 整个短场景在手机视口内垂直居中。
- 其他独立页面仍然顶部对齐。

### 图表与地图

- 数据图表保留点击触发动画。
- 政策地图圆点可点击并打开详情面板。
- 地图详情面板打开时，分页手势暂停。

### 加载与返回

- 首次打开先显示加载页，加载完成后播放开头动画。
- 结尾“点击返回开头”会回到顶部并重新加载动画。

## 5. 关键文件职责

- `interaction-core.js`：前半段纯动画计算。
- `app-interactions.js`：前半段场景渲染、气泡点击、手机过渡。
- `extension-core.js`：后半段动画计算。
- `extension.js`：后半段场景构建与进度。
- `extension-charts.js`：SVG 图表。
- `extension-map.js`：世界政策地图及详情面板。
- `mobile-pager.js`：手机端翻页顺序、手势、采访连续滚动和遮罩。
- `site-shell.js`：加载页、动画帧释放和返回开头。

## 6. 修改后的最低检查

1. 使用手机宽度测试，至少覆盖 375×667、390×844、430×932。
2. 检查网络环境只出现一次，且只由点击手机触发。
3. 检查采访引入可以连续滑到三个家庭，并能继续进入数据页。
4. 检查普通页面一次上滑只切换一页。
5. 检查卧室、教室、放学路的第二气泡仍可点击出现。
6. 检查地图详情打开时不会误翻页。
7. 检查结尾按钮能返回开头。
8. 检查 `prefers-reduced-motion` 模式。
9. 不要覆盖或替换 `assets/` 中的原始导出图层，除非用户明确提供了新素材。

